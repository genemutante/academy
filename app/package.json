{
  "name": "academy-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "prebuild": "echo 🚀 INICIANDO BUILD NUXT && ls -la && cat nuxt.config.ts",
   "build": "nuxi build --verbose",
    "generate": "nuxi generate"
  },
"engines": {
  "node": "18.x"
},
  
  "dependencies": {
    "@supabase/supabase-js": "^2.39.5",
    "axios": "^1.6.7",
    "nuxt": "^3.9.1",
     "nuxi": "latest"
  }
}

